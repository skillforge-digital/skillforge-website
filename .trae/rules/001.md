STRICT RULE 1: ZERO VISUAL/ASSET MODIFICATION
You must not change, move, or delete any CSS (styles), JavaScript (assets), images, or layout structures. All inlined <style> and <script> blocks must remain exactly as they are in the source code.

STRICT RULE 2: ROUTING-ONLY TRANSITION
Your only task is to replace the Single-Page Application (SPA) hash-routing system (e.g., #about, showSection()) with a global Multi-Page Application (MPA) folder structure (e.g., /about-us/, /academy/).

STRICT RULE 3: COMPONENT INTEGRITY
Do not remove any page sections such as the Footer, Feedback form, or Newsletter blocks. These must remain on the landing page and all sub-pages exactly as they appear in the original index.html.

STRICT RULE 4: PATH ACCURACY
You must ensure all internal links use correct relative pathing (e.g., ../../) based on the folder depth to ensure navigation works perfectly across the entire MPA structure.

IF YOU CANNOT PERFORM THE ROUTING CHANGE WITHOUT MODIFYING STYLES OR ASSETS, STOP AND ASK FOR CLARIFICATION. DO NOT PROCEED WITH AUTOMATIC CLEANUP.
